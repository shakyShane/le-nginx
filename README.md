> forked from https://bitbucket.org/automationlogic/le-docker-compose
 
Added:

- alpine images
- different way of killing nginx (needed for alpine image)
- `docker-cloud.yml` example showing a copy/paste version you can use as a stackfile